openapi: 3.1.0
info:
  title: Example API
  version: 1.0.0

components:
  schemas:
    Order:
      type: object
      properties:
        id:
          type: integer
        orderStatus:
          type: string
          enum:
            - placed
            - processing
            - shipped
            - delivered
