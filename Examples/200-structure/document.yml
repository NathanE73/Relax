openapi: 3.1.0
info:
  title: Example API
  version: 1.0.0

components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        username:
          type: string
        bio:
          type: [string, "null"]
